% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corresp.R
\name{corresp}
\alias{corresp}
\title{Generate a treatment correspondence list}
\usage{
corresp(
  essai,
  mini,
  maxi,
  seed,
  statut,
  version,
  boi_label = NULL,
  col_widths = NULL,
  chemin = NULL
)
}
\arguments{
\item{essai}{Liste. Trial object created with \code{\link{init_essai}}.
For example: \code{essai <- init_essai(...)}.}

\item{mini}{Integer. First treatment box number.}

\item{maxi}{Integer. Last treatment box number.}

\item{seed}{Integer. Random seed used to ensure reproducibility.}

\item{statut}{Character string. Status of the generated list: \code{"FINALE"} or \code{"FICTIVE"}.}

\item{version}{Character string. Version of the generated list. For example, \code{"v01"}.}

\item{boi_label}{Character string. Custom label for treatment boxes.
If \code{NULL}, \code{"Boite de traitement numero xxx"} is used.
Only used with the REDCap system.}

\item{col_widths}{Character vector. Widths of the columns in the generated PDF table.
For example, \code{c("2cm", "3cm", "4cm")}. If \code{NULL}, column widths are automatically determined.}

\item{chemin}{Character string. Path to the output directory. If \code{NULL}, the current working directory is used.}
}
\value{
A data frame containing the generated treatment allocation list.
The list is also exported to files:
  \itemize{
    \item A data file (\code{.txt} for Ennov or \code{.csv} for REDCap).
    \item A PDF document including an institutional cover page.
    \item An Excel file for the Ennov system only.
  }
}
\description{
Generates a treatment allocation list: treatment box numbers/treatment
assignments, according to the randomization system defined in the
\code{essai} object created with \code{\link{init_essai}}. The generated
list is automatically exported in the appropriate formats, depending on
the selected system, and is also returned as a data frame for inspection.
}
\examples{
\dontrun{
essai <- init_essai(
  nom_etude = "ESSAI_ABC", circuit = "ennov", k = 2,
  block_sizes = c(4), nb_block = c(10),
  arm_label = c("Placebo", "Traitement"), arm_code = c(0, 1)
)
df <- corresp(essai, mini = 1, maxi = 50, seed = 42,
              statut = "FICTIVE", version = "v01")
head(df)
}

}
